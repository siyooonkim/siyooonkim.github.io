---
import type { CollectionEntry } from 'astro:content';
import FormattedDate from '../components/FormattedDate.astro';
import SiteLayout from './SiteLayout.astro';
import TableOfContents from '../components/TableOfContents.astro';
import PostCover from '../components/PostCover.astro';

type Props = CollectionEntry<'blog'>['data'];

interface Heading {
	depth: number;
	slug: string;
	text: string;
}

interface RelatedPost {
	slug: string;
	data: CollectionEntry<'blog'>['data'];
}

const { title, description, pubDate, updatedDate, heroImage, category, tags, headings = [], relatedPosts = [] } = Astro.props as Props & {
	headings?: Heading[];
	relatedPosts?: RelatedPost[];
};
---

<SiteLayout title={title} description={description}>
	<main class="article-shell">
		<article class="article-card">
			<header class="article-header">
				<p class="eyebrow">{category}</p>
				<h1>{title}</h1>
				<p class="description">{description}</p>
				<div class="meta-row">
					<div>
						<FormattedDate date={pubDate} />
						{updatedDate && <span class="updated"> · Updated <FormattedDate date={updatedDate} /></span>}
					</div>
					{tags.length > 0 && (
						<ul class="tag-list">
							{tags.map((tag: string) => <li>{tag}</li>)}
						</ul>
					)}
				</div>
			</header>

			<div class="hero-image">
				{heroImage ? <img src={heroImage} alt={title} /> : <PostCover data={{ title, description, pubDate, updatedDate, category, tags, heroImage }} />}
			</div>

			<div class="article-grid">
				<div class="prose"><slot /></div>
				<TableOfContents headings={headings} />
			</div>

			{relatedPosts.length > 0 && (
				<section class="related-posts">
					<p class="section-label">Related posts</p>
					<div class="related-grid">
						{relatedPosts.map((post: RelatedPost) => (
							<a href={`/posts/${post.slug}/`}>
								<strong>{post.data.title}</strong>
								<span>{post.data.description}</span>
							</a>
						))}
					</div>
				</section>
			)}
		</article>
	</main>
</SiteLayout>

<style>
	.article-shell {
		width: min(1040px, calc(100% - 56px));
		margin: 0 auto;
		padding: 2.6rem 0 2.2rem;
	}
	.article-card {
		display: grid;
		gap: 1.5rem;
	}
	.article-header {
		max-width: 680px;
	}
	.eyebrow,
	.section-label {
		margin: 0 0 1rem;
		font-size: 0.72rem;
		font-weight: 700;
		letter-spacing: 0.1em;
		text-transform: uppercase;
		color: var(--text-subtle);
	}
	h1 {
		margin: 0;
		font-size: clamp(1.72rem, 1.52rem + 0.7vw, 2.2rem);
		line-height: 1.05;
		letter-spacing: -0.02em;
	}
	.description {
		margin: 0.9rem 0 0;
		font-size: 0.9rem;
		color: var(--text-muted);
		line-height: 1.6;
	}
	.meta-row {
		margin-top: 1.1rem;
		display: flex;
		gap: 1rem;
		justify-content: space-between;
		align-items: center;
		flex-wrap: wrap;
		color: var(--text-subtle);
	}
	.updated {
		color: var(--text-subtle);
	}
	.tag-list {
		list-style: none;
		display: flex;
		gap: 0.5rem;
		padding: 0;
		margin: 0;
		flex-wrap: wrap;
	}
	.tag-list li {
		padding: 0.24rem 0.56rem;
		border-radius: 999px;
		background: rgba(255, 255, 255, 0.06);
		font-size: 0.74rem;
	}
	.hero-image :global(img),
	.hero-image :global(.generated-cover) {
		width: 100%;
		border-radius: 18px;
		border: 1px solid rgba(255, 255, 255, 0.08);
	}
	.article-grid {
		display: grid;
		grid-template-columns: minmax(0, 680px) 220px;
		gap: 1.25rem;
		align-items: start;
	}
	.prose {
		min-width: 0;
	}
	.related-posts {
		padding-top: 0.8rem;
		border-top: 1px solid rgba(255, 255, 255, 0.08);
	}
	.related-grid {
		display: grid;
		grid-template-columns: repeat(2, minmax(0, 1fr));
		gap: 1rem;
	}
	.related-grid a {
		display: grid;
		gap: 0.5rem;
		padding: 0.85rem 0.95rem;
		border-radius: 14px;
		background: rgba(255, 255, 255, 0.02);
		border: 1px solid rgba(255, 255, 255, 0.08);
		text-decoration: none;
		color: inherit;
	}
	.related-grid span {
		color: var(--text-muted);
		font-size: 0.88rem;
	}
	@media (max-width: 1040px) {
		.article-grid {
			grid-template-columns: 1fr;
		}
	}
	@media (max-width: 720px) {
		.article-shell {
			padding-top: 2.2rem;
		}
		.related-grid {
			grid-template-columns: 1fr;
		}
	}
</style>
